Caso não possua todas as bibliotecas instaladas, antes de executar o modelo
deve-se instalar o pacote de requerimentos contido nesse diretorio:

pip install -r requirements.txt
ou
conda install -r requirements.txt

*os arquivos foram desenvolvidos em Python3